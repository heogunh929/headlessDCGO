---
description: EX8-074 묶음 골 실행 — EX8-3 → EX8-1 → EX8-2 → 최종 EX8_074.cs 1:1 포팅 (순차, 각 단계 green 게이트)
---

EX8-074 묶음 골 진행. `docs/audit/ex8_074_remaining_goals.md`의 "EX8-074 — 묶음 goal" 스펙대로 아래 순서로 순차 실행:

1. EX8-3 (OnEndTurn 타이밍 등록)
2. EX8-1 ([When Digivolving] 동적-임계 suspend+delete)
3. EX8-2 ([All Turns] 1회/턴 자기 [When Digivolving] 재발동, EX8-1 의존)
4. 최종 EX8_074.cs 6 region 1:1 포팅 + 라이브 E2E

각 단계: 구현 전 원본 `DCGO/Assets/Scripts/CardEffect/EX8/Green/EX8_074.cs`(+해당 원본 규칙)를 1:1 확인(추측 금지) → probe(엔진에 이미 있는지) → 원본구조 미러 → `bash scripts/run-tests.sh` 전체 green + 발동/동작 단언 라이브 테스트 + `tools/RuleAudit` 위반 0.
이전 단계가 완전히 green(FAIL=0)이 된 뒤에만 다음 단계로. AS-IS 규칙이 불명확하면 중단·확인. 커밋은 내가 지시할 때(단계별 커밋 권장).
