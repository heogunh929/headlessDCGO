# L0 — Mode A 최소형: 고정 덱 2개, 리그 off (dev design §2, roadmap L0)
# 레시피 경로는 rl/ 기준 상대경로. 실제 레시피는 운영자가 던진다(FR-3.1) — 외부 텍스트/내부 JSON 둘 다 허용.
experiment: l0-fixed-pair
seed: 42
deck_source:
  type: fixed
  recipes:
    - decks/st1_red.json
    - decks/st2_blue.json
policy:
  arch: mlp
  init: null
  learn: true
reward:
  type: terminal
league:
  enabled: false
log_level: RESULT
parallel:
  n_envs: 4
