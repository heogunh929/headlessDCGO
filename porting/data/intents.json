{
  "schema": "intents/1",
  "tableVer": "intents@2026-07-04",
  "_doc": "코루틴 의도 → 트리거 팩토리 매핑(pipeline-v3 stage 3 활성효과 로워링). ActivateClass 빌더는 헤드리스에서 단일 TriggerEffect 팩토리로 접힌다. yield 된 코루틴 본문 호출(예 card.Owner.AddMemory(N))을 factory + 인자 배치로 변환. 단일 yield + 매핑 존재 + activateCondition 이 배관만일 때 결정론. 그 외는 STOP.",
  "intents": {
    "card.Owner.AddMemory": {
      "factory": "AddMemoryTriggerEffect",
      "arg_map": {"amount": 0},
      "note": "[On X] Gain N memory — 트리거형 메모리 증가"
    },
    "CardEffectCommons.ChangeDigimonDP": {
      "factory": "SelfDpBuffTriggerEffect",
      "self_target_arg": 0,
      "arg_map": {"changeValue": 1, "duration": 2},
      "note": "[On X] this Digimon gets ±N DP — self DP 버프(트리거). 대상이 self 일 때만."
    }
  },
  "_activation_doc": "활성효과(IActivatedCardEffect) 코루틴 의도 → 활성 팩토리. TriggerEffect 와 달리 timing/condition/description 슬롯 없이 팩토리를 해당 타이밍 분기에 직접 add(옵션/시큐리티/on-play 등 activation flow 가 발화·게이팅). count_arg = ctorArgs 인덱스. WIRED 타이밍에서만 로워링, 그 외는 STOP(activation-pending). CanActivate 는 배관만일 때 드롭.",
  "activation_intents": {
    "DrawClass": { "factory": "DrawCardsEffect", "count_arg": 1, "note": "new DrawClass(owner, N, ac).Draw → DrawCardsEffect(card, N)" }
  },
  "activation_wired_timings": ["BeforePayCost", "OnEnterFieldAnyone", "OptionSkill", "SecuritySkill", "WhenDigivolving"],
  "plumbing_predicates": [
    "CanActivateOnDeletion", "CanActivateOnPlay", "CanActivateOnAttack",
    "CanActivateOnDigivolve", "CanActivateOnStartTurn", "CanActivateOnEndTurn",
    "CanTriggerOnDeletion", "CanTriggerOnPlay", "CanTriggerOnAttack",
    "CanTriggerOnDigivolve", "CanTriggerOnBlock",
    "CanAddMemory"
  ],
  "_plumbing_doc": "activateCondition 안의 배관 술어 — 헤드리스 TriggerEffect 팩토리가 timing/triggerGate 로 내부 처리하므로 condition 에서 제거. 전부 배관이면 condition=null. 의미 술어가 남으면 그것만 lower."
}
