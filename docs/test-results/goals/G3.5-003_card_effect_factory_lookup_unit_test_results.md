# G3.5-003 CardEffectFactory Lookup player/context Unit Test Results

## 실행 일시

- 2026-06-25 22:30:24 +09:00

## Goal 범위

- Goal ID: G3.5-003
- 목표: `CardEffectFactoryBindingRegistry.Lookup`의 player 1 하드코딩 제거 (B-01 수정)
- 작업 범위: player/context를 받는 `Lookup` overload 추가, 기존 2-인자 overload 격리(`[Obsolete]`), player별 binding 정확성 테스트 보강
- 산출물: `Lookup(card, trigger, controllerId, context)` overload, 격리된 legacy overload
- 완료 기준: 임의 player(예: player 2)로 lookup/bind 시 controller가 정확히 전달되고, 하드코딩 player 1은 격리된 obsolete 경로로만 남음
- 선행 Goal 확인: `G3.5-002_match_state_mutation_sink_unit_test_results.md`에서 COMPLETE 확인
- 근거 문서: `docs/phase3_5_integration_design.md` (P0 / G3.5-003, P0-3), `docs/audit/phase3_parity_audit_report.md` (B-01)

## 문제 (AS-IS)

- `CardEffectFactoryBindingRegistry.Lookup(CardRecord, string)`가 `new HeadlessPlayerId(1)`을 controller/context로 하드코딩했다 (`CardEffectFactoryBinding.cs:247-248`).
- goal 단위테스트(G3J-001)가 이 경로를 검출하지 못한 API 설계 버그였고, player 2 관점 binding lookup이 잘못된 controller로 흐를 수 있었다.

## 수정/생성 파일

- 수정: `src/HeadlessDCGO.Engine/Headless/Effects/CardEffectFactoryBinding.cs` (player/context overload 추가, 2-인자 overload `[Obsolete]` 격리)
- 생성: `tests/G3.5-003.CardEffectFactory.lookup.Tests/G3.5-003.CardEffectFactory.lookup.Tests.csproj`
- 생성: `tests/G3.5-003.CardEffectFactory.lookup.Tests/Program.cs`
- 생성: `docs/test-results/goals/G3.5-003_card_effect_factory_lookup_unit_test_results.md`

## 참조한 AS-IS 파일

- 읽기 전용 확인: `src/HeadlessDCGO.Engine/Assets/Scripts/Script/CardEffectFactory/CardEffectFactoryBinding.cs` (facade는 이미 player/context를 받음 → 변경 불필요)
- 원본 `DCGO/Assets/...` 파일 수정 없음

## 구현 요약

- `Lookup(CardRecord card, string trigger, HeadlessPlayerId controllerId, EffectContext context)` overload를 추가했다. 호출자가 제공한 controller/context로 `CardEffectFactoryBindingRequest`를 구성한다.
- 기존 2-인자 `Lookup(card, trigger)`는 `[Obsolete]`로 표기하고 내부적으로 새 overload에 위임하도록 격리했다. 하드코딩 player 1은 이 legacy 경로에만 남는다.
- Assets facade(`CardEffectFactoryBinding.Bind`)는 이미 controller/context를 받으므로 변경하지 않았다.

## 테스트 명령

```powershell
.\.dotnet\dotnet.exe run --project .\tests\G3.5-003.CardEffectFactory.lookup.Tests\G3.5-003.CardEffectFactory.lookup.Tests.csproj
```

## 테스트 결과

- 전체: 7
- 통과: 7
- 실패: 0
- 스킵: 0

통과한 테스트:

- Player/context Lookup overload returns matched rules
- Bind carries the requesting player's controller id
- Lookup overload uses the supplied controller, not hardcoded player 1
- Legacy two-argument Lookup is marked obsolete
- Legacy two-argument Lookup still matches rules
- Lookup overload validates the context argument
- Binding source has no Unity dependency or placeholder

## 회귀 검증 (영향받는 기존 스위트)

- `G3J-001.CardEffectFactory.binding.Tests`: 10/10 통과
- `G3J-002.PermanentEffectFactory.binding.Tests`: 10/10 통과

## 실패 상세

- 최종 실행 실패 없음

## 테스트 중 관찰된 경고

- 최종 실행에 기존 `MetadataActionProcessor.cs`의 nullable 경고(CS8604)가 남아 있으나, 이번 Goal 변경 범위 밖 파일이다.
- legacy obsolete overload 직접 호출 테스트는 `#pragma warning disable CS0618`로 의도적으로 감쌌다.

## 테스트하지 못한 항목과 이유

- runtime에서 player별 binding을 실제로 구동하는 통합은 공통 루프(G3.5-004 이후) 범위다. 현재 2-인자 Lookup은 어떤 런타임 경로에서도 호출되지 않으므로(잠재 버그), 이번 Goal은 API 정정 + player 정확성 단위검증까지 수행했다.

## 미해결 리스크

- legacy obsolete overload는 하위호환을 위해 유지된다. 모든 호출처가 새 overload로 이전되면 제거 가능하다.

## 다음 Goal 진행 가능 여부

- G3.5-003 완료로 **P0 3건(B-01/B-02/B-03)이 모두 마무리**되었으므로, G3.5-004(GameFlowProcessor 공통 루프 골격) 진행 가능

## 완료 판정

- COMPLETE
